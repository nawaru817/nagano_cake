<h1>商品一覧</h1>(全<%= Item.count %>件)<br>

<% @items.each do |item| %>
  <%= link_to (attachment_image_tag item, :image, :fill, 100, 100, fallback: "no_image.jpg"), item_path(item) %><br>
  <%= item.name %><br>
  ￥<%= (item.price).to_s(:delimited) %><br>
<% end %>

<%= paginate @items %>